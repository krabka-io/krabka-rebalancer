# Broker integration suites

These suites came from the original monorepo and require the unpublished
`krabka-broker` test-helper API. They remain here as the cross-repository
contract while the broker publishes or exposes a standalone test fixture.

The standalone CI gate runs the crate's unit and binary tests.
